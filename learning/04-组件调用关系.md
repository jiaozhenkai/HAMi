# 组件之间的调用关系

本文档说明 HAMi 各组件之间的调用关系、接口边界和数据流向。理解组件如何协作，是深入 HAMi 的关键。

## 一、组件交互总览

```mermaid
graph TB
    subgraph 外部["外部系统"]
        API["kube-apiserver"]
        KL["kubelet"]
        KSC["kube-scheduler"]
        PROM["Prometheus"]
    end

    subgraph SchedulerPod["Scheduler Pod"]
        WH["Webhook Handler<br/>/webhook"]
        SE["Scheduler Extender<br/>/filter /bind /refit"]
        DM["device.DevicesMap<br/>16 个后端"]
    end

    subgraph DevicePluginPod["Device Plugin Pod (每节点)"]
        DP["Device Plugin<br/>gRPC Server"]
        VM["vGPU Monitor"]
    end

    subgraph Container["用户容器"]
        LIB["libvgpu.so"]
        APP["应用进程"]
        SHM["共享内存 .cache"]
    end

    %% 调度阶段
    KSC -->|"1. Extender API<br/>POST /filter"| SE
    KSC -->|"POST /bind"| SE
    API -->|"0. Admission"| WH
    SE -->|"2. 读写 Pod/Node"| API
    SE -->|"3. 调用 Fit/Score"| DM
    DM -->|"读写节点设备注解"| API

    %% 分配阶段
    KL -->|"4. gRPC Allocate"| DP
    DP -->|"5. 读 Pod 注解"| API
    DP -->|"6. 设置环境变量+挂载"| KL
    DP -->|"创建 .cache 文件"| SHM

    %% 运行时
    KL -->|"7. 启动容器"| LIB
    APP -->|"8. CUDA 调用"| LIB
    LIB -->|"读写使用量"| SHM
    VM -->|"9. mmap 读取"| SHM
    VM -->|"写控制信号"| SHM
    LIB -->|"读控制信号"| SHM
    VM -->|"10. NVML"| VM
    VM -->|":9394 metrics"| PROM
    SE -->|":9395 metrics"| PROM

    %% 节点锁
    SE -.->|"hami.io/mutex.lock"| API
    DP -.->|"读取锁找 Pod"| API

    style SE fill:#4A90D9,color:#fff
    style DP fill:#50C878,color:#fff
    style VM fill:#F5A623,color:#fff
    style LIB fill:#9B59B6,color:#fff
    style SHM fill:#BD10E0,color:#fff
```

## 二、接口边界与通信协议

HAMi 各组件之间通过 **4 种通信机制**协作：

```mermaid
graph LR
    subgraph 通信机制
        A["① K8s API (Pod/Node 注解)"]
        B["② HTTP (Extender API)"]
        C["③ gRPC (Device Plugin API)"]
        D["④ 共享内存 (.cache 文件)"]
    end

    SE["Scheduler"] -.->|"① 写分配注解<br/>读设备清单注解"| API2["kube-apiserver"]
    KSC2["kube-scheduler"] -->|"② /filter /bind"| SE
    KL2["kubelet"] -->|"③ Allocate"| DP2["Device Plugin"]
    VM2["Monitor"] <-->|"④ mmap"| SHM2["共享内存"]
    LIB2["libvgpu.so"] <-->|"④ mmap"| SHM2

    style A fill:#4A90D9,color:#fff
    style B fill:#50C878,color:#fff
    style C fill:#F5A623,color:#fff
    style D fill:#BD10E0,color:#fff
```

| 通信机制 | 使用方 | 方向 | 载体 |
|---------|--------|------|------|
| ① K8s 注解 | Scheduler ↔ API Server ↔ Device Plugin | 双向 | Pod/Node annotations (`hami.io/`) |
| ② HTTP Extender API | kube-scheduler → Scheduler Extender | 请求-响应 | JSON over HTTP (ExtenderArgs) |
| ③ gRPC Device Plugin API | kubelet → Device Plugin | 请求-响应 | gRPC (ListAndWatch, Allocate) |
| ④ 共享内存 | libvgpu.so ↔ vGPU Monitor | 双向 | mmap 的 `.cache` 文件 |

---

## 三、注解契约——组件间的"API"

**注解是 HAMi 组件协作的核心契约**。Scheduler 写入，Device Plugin 读取，Monitor 间接依赖。下面是完整的注解体系。

### 3.1 Pod 生命周期注解

```mermaid
graph LR
    subgraph 写入方["Scheduler 写入"]
        VN["hami.io/vgpu-node<br/>分配的目标节点"]
        VT["hami.io/vgpu-time<br/>分配时间戳"]
        BT["hami.io/bind-time<br/>绑定时间戳"]
        BP["hami.io/bind-phase<br/>allocating/success/failed"]
    end

    subgraph 读取方["Device Plugin 读取"]
        GetPod["GetPendingPod()<br/>通过 bind-phase=allocating 找 Pod<br/>通过 vgpu-node 匹配节点"]
    end

    VN --> GetPod
    BP --> GetPod
    BT --> GetPod

    style VN fill:#4A90D9,color:#fff
    style BP fill:#50C878,color:#fff
```

### 3.2 设备分配注解（核心契约）

这是 Scheduler 和 Device Plugin 之间最重要的数据传递通道。每个设备后端有 **to-allocate**（调度器写入，待执行）和 **allocated**（已确认分配）两个注解：

| 厂商 | to-allocate（调度器→插件） | allocated（确认） |
|------|---------------------------|-------------------|
| NVIDIA | `hami.io/vgpu-devices-to-allocate` | `hami.io/vgpu-devices-allocated` |
| AMD | `hami.io/amd-devices-to-allocate` | `hami.io/amd-devices-allocated` |
| 寒武纪 | `hami.io/cambricon-mlu-devices-to-allocate` | `hami.io/cambricon-mlu-devices-allocated` |
| 海光 | `hami.io/hcu-devices-to-allocate` | `hami.io/hcu-devices-allocated` |
| 昆仑 | `hami.io/xpu-devices-to-allocate` | `hami.io/xpu-devices-allocated` |
| 摩尔线程 | `hami.io/mthreads-vgpu-devices-to-allocate` | `hami.io/mthreads-vgpu-devices-allocated` |
| 启英 | `hami.io/enflame-gcu-devices-to-allocate` | `hami.io/enflame-gcu-devices-allocated` |

分配注解的编码格式（以 NVIDIA 为例）：
```
GPU_ID,类型,已用显存,已用核心,UUID;GPU_ID,类型,...
```

### 3.3 节点设备注册注解（Device Plugin → Scheduler）

Device Plugin 定期上报节点设备清单，Scheduler 读取并缓存：

| 注解 | 写入方 | 读取方 | 内容 |
|------|--------|--------|------|
| `hami.io/node-nvidia-register` | Device Plugin | Scheduler `GetNodeDevices()` | JSON 编码的 GPU 清单（ID/显存/核心/型号/MIG） |
| `hami.io/node-handshake` | Device Plugin | Scheduler `CheckHealth()` | 握手心跳，证明插件存活 |
| `hami.io/node-nvidia-score` | Device Plugin | Scheduler | GPU 间 P2P/NVLink 拓扑评分 |

其他厂商模式类似：`hami.io/node-{vendor}-register`、`hami.io/node-handshake-{vendor}`。

### 3.4 节点锁注解

| 注解 | 写入方 | 读取方 | 值格式 |
|------|--------|--------|--------|
| `hami.io/mutex.lock` | Scheduler `LockNode()` | Scheduler `LockNode()` | `RFC3339时间戳,ns,pod` |

### 3.5 调度策略注解（用户 → Scheduler）

| 注解 | 作用 | 可选值 |
|------|------|--------|
| `hami.io/node-scheduler-policy` | 覆盖节点策略 | `binpack` / `spread` |
| `hami.io/gpu-scheduler-policy` | 覆盖 GPU 策略 | `binpack`/`spread`/`numa`/`mutex`/`topology-aware`，可逗号组合 |
| `hami.io/device-scoring-weights` | 自定义评分权重 | `slot=1,core=1,memory=3` |
| `hami.io/numa-alignment` | NUMA 对齐模式 | `best-effort` / `strict` |
| `hami.io/device-cordon` | 隔离指定 GPU | 逗号分隔的 GPU UUID 列表 |

### 3.6 MIG 专用注解

| 注解 | 作用 |
|------|------|
| `hami.io/vgpu-mig-allocations` | JSON 编码的 MIG 分配详情（profile/placement/GI/CI ID） |

---

## 四、Scheduler 内部调用关系

```mermaid
graph TD
    subgraph 路由层
        R["routes.PredicateRoute()"]
        RB["routes.Bind()"]
        RW["routes.WebHookRoute()"]
    end

    subgraph 业务层["Scheduler 结构体方法"]
        F["Filter()"]
        B["Bind()"]
        RF["RefitNumaAllocation()"]
    end

    subgraph 评分层
        CS["calcScore()"]
        SN["scoreNode()"]
        FID["fitInDevices()"]
    end

    subgraph 缓存层
        NM["nodeManager"]
        PM["podManager"]
        QM["quotaManager"]
        OS["overviewstatus"]
    end

    subgraph 设备层["device.DevicesMap (16 后端)"]
        FIT["Fit()"]
        SCR["ScoreNode()"]
        ARU["AddResourceUsage()"]
        PA["PatchAnnotations()"]
        MA["MutateAdmission()"]
        GND["GetNodeDevices()"]
        GRR["GenerateResourceRequests()"]
        LN["LockNode()/ReleaseNodeLock()"]
        CH["CheckHealth()"]
    end

    subgraph 工具层
        NL["nodelock.LockNode()"]
        RR["device.Resourcereqs()"]
        GNU["getNodesUsage()"]
    end

    R --> F
    RB --> B
    RW --> MA

    F --> RR --> GRR
    F --> GNU --> NM & PM
    F --> CS --> SN --> FID --> FIT
    FID --> ARU
    SN --> SCR
    F --> PA
    F --> PM & QM

    B --> LN
    B --> PM

    RF --> PM

    NM -.->|"后台同步"| GND & CH

    style F fill:#4A90D9,color:#fff
    style FID fill:#F5A623,color:#fff
    style NL fill:#D0021B,color:#fff
    style FIT fill:#9B59B6,color:#fff
```

### 调用链路文字描述

| 触发点 | 调用链 |
|--------|--------|
| Webhook | `WebHookRoute()` → `webhook.Handle()` → 各后端 `MutateAdmission()` → `fitResourceQuota()` |
| Filter | `PredicateRoute()` → `Filter()` → `Resourcereqs()`→`GenerateResourceRequests()` → `getNodesUsage()`→`nodeManager`+`podManager` → `calcScore()`→`scoreNode()`→`fitInDevices()`→`Fit()`+`AddResourceUsage()` → `PatchAnnotations()` → 写入 `podManager`+`quotaManager` |
| Bind | `Bind()` → `lockAllDevices()`→各后端`LockNode()`→`nodelock.LockNode()` → `kubeClient.Bind()` |
| 缓存同步 | `RegisterFromNodeAnnotations()` → `GetNodeDevices()`→`CheckHealth()`→`nodeManager.addNode()` |

---

## 五、Scheduler 与 Device Plugin 的协作

两个组件**不直接通信**，而是通过 kube-apiserver 上的 Pod 注解间接协作：

```mermaid
sequenceDiagram
    autonumber
    participant SE as "Scheduler Extender"
    participant API as "kube-apiserver"
    participant KL as "kubelet"
    participant DP as "Device Plugin"

    rect rgb(74, 144, 217, 0.1)
        Note over SE: Filter 阶段: 写"决策"
        SE->>SE: Fit() + 评分 → 确定分配方案
        SE->>API: patch Pod 注解:<br/>• hami.io/vgpu-node = 节点名<br/>• hami.io/vgpu-devices-to-allocate = 编码方案<br/>• hami.io/vgpu-time = 时间戳
    end

    rect rgb(74, 144, 217, 0.1)
        Note over SE: Bind 阶段: 写"执行信号"
        SE->>API: patch Pod 注解:<br/>• hami.io/bind-phase = "allocating"<br/>• hami.io/bind-time = 时间戳
        SE->>API: K8s Bind() 绑定 Pod→Node
    end

    API->>KL: Pod 绑定到节点

    rect rgb(80, 200, 120, 0.1)
        Note over DP: Allocate 阶段: 读"决策"并执行
        KL->>DP: gRPC Allocate()
        DP->>API: GetPendingPod() 找 bind-phase=allocating 的 Pod
        DP->>API: 读取 hami.io/vgpu-devices-to-allocate
        DP->>DP: 解码分配方案, 设置环境变量
        DP->>API: patch bind-phase = "success"
        DP->>API: patchErasedAnnotation() 擦除 to-allocate
        DP-->>KL: AllocateResponse
    end

    Note over SE,DP: 关键: 两组件通过 Pod 注解"对话"<br/>Scheduler 是"决策者", Device Plugin 是"执行者"
```

### 数据流总结

```
Scheduler 决策:
  hami.io/vgpu-node          → 告诉 DP "这个 Pod 归我管, 分到这个节点"
  hami.io/vgpu-devices-to-allocate → 告诉 DP "用这些 GPU, 每个用多少显存/核心"
  hami.io/bind-phase=allocating    → 告诉 DP "现在可以执行分配了"

Device Plugin 执行:
  读取 to-allocate 注解      → 知道该挂载哪些 GPU
  设置 CUDA_DEVICE_MEMORY_LIMIT → 通知 libvgpu.so 限制显存
  创建 .cache 文件           → 建立与 Monitor 的共享内存通道
  写 bind-phase=success      → 通知 Scheduler "分配完成"
```

---

## 六、Device Plugin 与 vGPU Monitor 的协作

两个组件运行在**同一个 DaemonSet Pod**中，通过共享卷和共享内存协作：

```mermaid
graph TD
    subgraph DevicePluginPod["Device Plugin Pod (同一 Pod)"]
        DP["Device Plugin 容器"]
        VM["vGPU Monitor 容器"]
    end

    subgraph 共享通道
        SHM["共享内存 .cache<br/>{HOOK_PATH}/vgpu/containers/"]
        MIG["MIG Apply Lock 文件"]
    end

    subgraph 容器内
        LIB["libvgpu.so"]
    end

    DP -->|"创建 .cache 文件"| SHM
    DP -->|"挂载 libvgpu.so"| LIB
    DP -->|"创建 MIG 锁"| MIG
    VM -->|"mmap 读取使用量"| SHM
    VM -->|"写控制信号"| SHM
    LIB -->|"读写使用量"| SHM
    LIB -->|"读控制信号"| SHM
    VM -->|"检测锁, 暂停反馈"| MIG

    style SHM fill:#BD10E0,color:#fff
    style MIG fill:#D0021B,color:#fff
```

### 共享内存协议

```mermaid
graph LR
    subgraph cache[" .cache 文件结构"]
        H["headerT<br/>magic: 19920718<br/>version: v0/v1"]
        U["UsageInfo<br/>显存使用/核心使用/<br/>最近内核时间/优先级/<br/>阻塞标志/限流标志"]
    end

    DP["Device Plugin"] -->|"创建并初始化"| H
    LIB["libvgpu.so"] -->|"写入实际使用量"| U
    LIB -->|"读取控制标志"| U
    VM["vGPU Monitor"] -->|"mmap 读取"| U
    VM -->|"写入控制信号"| U

    style U fill:#BD10E0,color:#fff
```

---

## 七、设备后端接口调用关系

所有 16 个设备后端实现相同的 `Devices` 接口，调度器通过 `DevicesMap` 统一调用：

```mermaid
graph TD
    subgraph 调度器调用点
        C1["Webhook 变更<br/>调用 MutateAdmission()"]
        C2["资源解析<br/>调用 GenerateResourceRequests()"]
        C3["节点设备发现<br/>调用 GetNodeDevices()"]
        C4["健康检查<br/>调用 CheckHealth()"]
        C5["过滤适配<br/>调用 Fit()"]
        C6["评分<br/>调用 ScoreNode()"]
        C7["记录分配<br/>调用 AddResourceUsage()"]
        C8["写注解<br/>调用 PatchAnnotations()"]
        C9["节点锁<br/>调用 LockNode/ReleaseNodeLock()"]
        C10["资源名<br/>调用 GetResourceNames()"]
        C11["节点清理<br/>调用 NodeCleanUp()"]
        C12["标识<br/>调用 CommonWord()"]
    end

    DM["DevicesMap<br/>map[commonWord]Devices"]

    NV["NvidiaGPUDevices"]
    MLU["CambriconDevices"]
    ASC["Ascend Devices[]"]
    OTHER["...其余 13 个"]

    C1 & C2 & C3 & C4 & C5 & C6 & C7 & C8 & C9 & C10 & C11 & C12 --> DM
    DM --> NV & MLU & ASC & OTHER

    style DM fill:#4A90D9,color:#fff
    style C5 fill:#F5A623,color:#fff
    style C8 fill:#50C878,color:#fff
```

### 各后端实现的差异点

| 后端 | Fit() 复杂度 | ScoreNode() | 特殊能力 |
|------|-------------|-------------|---------|
| NVIDIA | 最高（~170行） | 返回 0（评分在 Fit 内做） | MIG 规划、P2P 拓扑、NUMA、配额、cordon |
| Metax SGPU | 中 | 返回拓扑分（`policyNeutralScorer`） | 拓扑感知 |
| Ascend | 中 | 返回 0 | 多芯片、superpod、模板配额 |
| AMD/寒武纪/海光等 | 低 | 返回 0 | 基本显存/核心隔离 |

> **设计要点**：`ScoreNode()` 默认返回 0，多数后端不实现自定义评分，评分逻辑统一在 `policy` 层。只有需要特殊拓扑评分的后端（如 Metax SGPU）才实现 `policyNeutralScorer` 接口，其评分通过 `OverrideScore()` 以 ×10000 权重叠加。

---

## 八、数据流总图

```mermaid
graph LR
    subgraph 输入["输入数据"]
        P1["Pod Spec<br/>资源请求"]
        P2["Node 注解<br/>设备清单"]
        P3["缓存 Pod 注解<br/>已分配方案"]
    end

    subgraph 处理["Scheduler 处理"]
        P["解析请求"]
        F["过滤评分"]
        D["决策分配方案"]
    end

    subgraph 输出["输出数据"]
        O1["Pod 注解<br/>分配方案 (to-allocate)"]
        O2["缓存<br/>podManager/quotaManager"]
        O3["Node 注解<br/>mutex.lock"]
    end

    subgraph 执行["Device Plugin 执行"]
        E1["读取注解"]
        E2["设置环境变量"]
        E3["挂载 libvgpu.so"]
        E4["创建 .cache"]
    end

    subgraph 运行时["运行时"]
        R1["libvgpu.so 拦截 CUDA"]
        R2["Monitor 监控+控制"]
        R3["Prometheus 指标"]
    end

    P1 --> P --> F --> D
    P2 --> F
    P3 --> F
    D --> O1 & O2 & O3
    O1 --> E1 --> E2 & E3 & E4
    E3 --> R1
    E4 --> R2
    R1 --> R3
    R2 --> R3

    style F fill:#4A90D9,color:#fff
    style D fill:#F5A623,color:#fff
    style E2 fill:#50C878,color:#fff
    style R1 fill:#9B59B6,color:#fff
```

---

## 九、关键依赖关系（谁依赖谁）

```mermaid
graph TD
    SE["Scheduler Extender"] -->|"依赖"| DM2["device.DevicesMap<br/>设备后端"]
    SE -->|"依赖"| NL2["nodelock<br/>节点锁"]
    SE -->|"依赖"| LE2["leaderelection<br/>选举"]
    SE -->|"依赖"| PO2["policy<br/>调度策略"]
    SE -->|"依赖"| CL["util/client<br/>K8s 客户端"]

    DP["Device Plugin"] -->|"依赖"| NVML["NVML<br/>GPU 库"]
    DP -->|"依赖"| RM2["resource manager"]
    DP -->|"依赖"| CDI["CDI 规范"]
    DP -->|"依赖"| CL

    VM["vGPU Monitor"] -->|"依赖"| NVML
    VM -->|"依赖"| SHM2["共享内存协议"]
    VM -->|"依赖"| DP2["MIG 锁协调"]

    DM2 -->|"实现"| BACK["16 个厂商后端"]
    BACK --> NVB["nvidia (最复杂)"]

    style SE fill:#4A90D9,color:#fff
    style DP fill:#50C878,color:#fff
    style VM fill:#F5A623,color:#fff
    style DM2 fill:#9B59B6,color:#fff
```

### 依赖说明
- **Scheduler** 依赖设备后端（调度决策）、节点锁（并发控制）、选举（高可用）、策略（评分）
- **Device Plugin** 依赖 NVML（GPU 发现）、CDI（容器规范）、K8s 客户端（读注解）
- **vGPU Monitor** 依赖 NVML（宿主指标）、共享内存（容器指标）、MIG 锁文件（协调）
- **三者共同依赖** `libvgpu.so`（运行时隔离）和 `pkg/util/client`（K8s 客户端）

---

> **相关文档**：[05-学习路线.md](./05-学习路线.md) 规划如何系统地学习这些组件。
